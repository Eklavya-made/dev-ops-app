kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
- role: control-plane
  extraPortMappings:
  # HTTP (Ingress)
  - containerPort: 80
    hostPort: 9090
    protocol: TCP

  # HTTPS (optional, future use)
  - containerPort: 443
    hostPort: 9443
    protocol: TCP
